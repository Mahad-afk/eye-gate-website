<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Eye Gate - Sunglasses</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Poppins:400,600&display=swap">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background-color: #f3f4f6;
            color: #111827;
        }

        header {
            background-color: #1f2937;
            color: #fff;
            padding: 20px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
        }

        nav a {
            color: white;
            margin-left: 20px;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s;
        }

        nav a:hover {
            color: #facc15;
        }

        .hero {
            background: linear-gradient(to right, rgba(0,0,0,0.5), rgba(0,0,0,0.2)), url('https://images.unsplash.com/photo-1610340940708-97b0c9a2e008?auto=format&fit=crop&w=1950&q=80') no-repeat center center/cover;
            height: 80vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            text-align: center;
        }

        .hero h1 {
            font-size: 52px;
            background-color: rgba(0, 0, 0, 0.6);
            padding: 20px 30px;
            border-radius: 12px;
            line-height: 1.4;
        }

        .features, .products, .footer, .contact {
            padding: 60px 40px;
            text-align: center;
        }

        .features h2, .products h2, .contact h2 {
            margin-bottom: 40px;
            font-size: 36px;
            color: #111;
        }

        .features .grid, .products .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
        }

        .feature-box, .product {
            background-color: white;
            border-radius: 12px;
            padding: 25px;
            box-shadow: 0 4px 16px rgba(0,0,0,0.08);
            transition: transform 0.2s;
        }

        .feature-box:hover, .product:hover {
            transform: translateY(-5px);
        }

        .product img {
            width: 100%;
            border-radius: 8px;
            margin-bottom: 15px;
        }

        .product button {
            margin-top: 10px;
            padding: 10px 15px;
            background-color: #1f2937;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 6px;
            transition: background-color 0.3s;
        }

        .product button:hover {
            background-color: #facc15;
            color: black;
        }

        .footer {
            background-color: #1f2937;
            color: #d1d5db;
            padding: 40px;
        }

        .cart {
            position: fixed;
            bottom: 20px;
            right: 20px;
            background-color: #facc15;
            color: #000;
            padding: 12px 24px;
            border-radius: 999px;
            font-weight: bold;
            cursor: pointer;
            box-shadow: 0 4px 8px rgba(0,0,0,0.2);
            transition: transform 0.3s;
        }

        .cart:hover {
            transform: scale(1.05);
        }
    </style>
</head>
<body>
<!-- content remains unchanged -->
